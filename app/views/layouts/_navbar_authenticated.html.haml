%ul.nav.navbar-nav
  %li{class: ('active' if upload_page?)}= link_to 'Upload', new_upload_path
  %li{class: ('active' if merchant_page?)}= link_to 'Merchants', merchants_path

%ul.nav.navbar-nav.navbar-right
  %li.dropdown
    = link_to '#', class: 'dropdown-toggle', data: { toggle: 'dropdown' } do
      = current_user.email
      %b.caret
    %ul.dropdown-menu
      %li= link_to 'Account', edit_user_registration_path
      %li.divider
      %li= link_to 'Sign out', destroy_user_session_path, method: :delete
